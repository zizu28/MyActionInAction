name: "${{ github.ref }} Action In Action"
description: "Greets someone and returns 42 always"
inputs:
  who-to-greet:
    description: "Who to greet"
    required: true
    default: "World"
runs:
  using: "docker"
  image: "Dockerfile"
  args:
  -  ${{ inputs.who-to-greet }}
